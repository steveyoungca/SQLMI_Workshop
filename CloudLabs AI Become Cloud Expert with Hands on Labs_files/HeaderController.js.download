(function () {
    'use strict';
    angular
        .module('cloudsandbox.studentportal')
        .controller('HeaderController', HeaderController);

    HeaderController.$inject = ['$scope', '$location', 'adalAuthenticationService'];
    /// <summary>
    /// Controls login and logout
    /// </summary>
    function HeaderController($scope, $location, adalService) {
        var vm = this;
        $scope.showLogin = true;

        //if (adalService.userInfo.isAuthenticated) {
        //    $scope.showLogin = false;
        //    $scope.name = adalService.userInfo.profile.name;
        //}

        vm.login = function () {
            adalService.login();
        }

        vm.logout = function () {
            //adalService.logOut();
        }

    }

})();